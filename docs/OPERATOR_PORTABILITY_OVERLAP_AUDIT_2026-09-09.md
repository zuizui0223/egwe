# Operator portability overlap audit — 2026-09-09

## Question

Can the operator-portability Short Communication be developed as a publication candidate without duplicating the active NEE flagship?

## Active flagship ownership

The NEE flagship owns the following scientific chain:

`cross-layer covariance -> q-dependent allele sorting -> recruitment buffering / direct recoupling -> density-feedback failure gate -> continuous last-refuge fate ranking`.

Its load-bearing numerical evidence includes the `0.2543` matched-marginal transition counterexample, `+5.33/+5.20` horizon propagation contrasts, warning-denominator counts with specificity 0 / AUC 0.5, allele-sorting DID `+6.883`, exact route-sign audit `1,920,000/1,920,000`, and last-refuge AUC `0.92734`.

## Operator-portability ownership

The Short Communication owns only:

1. failure of the historical allele-only `m=.10` block-heterogeneity pattern to reproduce on one fresh independent seed ensemble;
2. failure of that historical pattern to appear under whole-individual `d=.10` substitution;
3. failure of that historical pattern to appear under pollen-only `g=.20` substitution;
4. the methodological boundary that a scalar connectivity label does not identify a transportable operator without a target-preserving mapping or calibrated equivalence;
5. the deterministic post-result precision boundary of the already completed five-block equal-rate tests.

## Explicit exclusions

The Short Communication must not use the flagship’s aligned/anti-aligned construction, operator-balance causal architecture, genetic-warning denominator result, route-margin warning result, last-refuge result, or natural-system projection narrative.

The submission-facing manuscript was audited for the following forbidden flagship tokens:

- `0.2543`
- `+5.33`
- `+5.20`
- `35/35`
- `48/48`
- `33/33`
- `49/49`
- `+6.883`
- `1,920,000`
- `0.92734`
- `+0.02135`

These tokens are permitted in this audit document only so that the firewall is explicit; they must be absent from `manuscript/operator_portability_short_communication.md`.

## State-validity fallback interaction

The frozen state-validity manuscript retains historical portability prose for provenance. If that fallback is ever reactivated, its portability material must be removed, subordinated or explicitly reconciled before submission. The current publication owner of the portability claim is the independent Short Communication candidate.

## Decision

**Development overlap audit: PASS.** The portability claim is scientifically separable from the active flagship and can be packaged in parallel. This is not blanket permission for simultaneous submission: the author must repeat the overlap/policy check using the then-current versions of both manuscripts immediately before submission.
